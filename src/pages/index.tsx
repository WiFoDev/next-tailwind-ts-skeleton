import type {NextPage} from "next";

import {Layout} from "@/Layout";

const Home: NextPage = () => {
  return (
    <Layout>
      <section>Welcome</section>
    </Layout>
  );
};

export default Home;
